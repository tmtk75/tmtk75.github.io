-#layout: default
#recent-posts
  {% for post in site.posts %}
  %article.post
    %header
      %h2
        .title {{post.title}}
        %small.date {{post.date}}
    .content {{post.content}}
  {% endfor %}

%script{src:"{{site.paths.vendor}}/jquery.js"}
%script{src:"{{site.paths.vendor}}/bootstrap/docs/assets/js/bootstrap-tooltip.js"}
